# >>> python을 켜고 사용 할 시 :
# type() -> 타입 검색할 때 쓸 수 있다
# type(7)라고 입력 -> class 'int'로 출력
# isinstance : 변수가 특정 타입의 객체를 가리키는지 확인할 때 씀
# isinstance(7, int) -> True / isinstance("야", str) -> True

# 변수 지정시 알파벳이더라도 ""가 붙지 않으면 int로 인식.
# 반대로 숫자더라도 ""를 붙이면 str로 인식.
